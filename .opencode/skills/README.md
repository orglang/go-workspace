# Local opencode skills

Place skill folders here. Each skill must live in its own directory and include
a `SKILL.md` file with frontmatter `name` and `description`.

Example structure:

```
.opencode/skills/my-skill/SKILL.md
```

This folder is referenced by `opencode.json` via `skills.paths`.
